services:
  paddleocr:
    build: .
    expose:
      - "8001"

    volumes:
      - ./models:/root/.paddlex
      
    networks:
      - nginx-demo-projects_demo-projects_network

    restart: unless-stopped
    
networks:
  nginx-demo-projects_demo-projects_network:
    external: true
